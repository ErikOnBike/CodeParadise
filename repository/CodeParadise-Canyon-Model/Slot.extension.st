Extension { #name : #Slot }

{ #category : #'*CodeParadise-Canyon-Model' }
Slot >> addPropertyTo: aCollection [

	"Add the receiver to the specified Collection.

	This method is used to generate methods describing the defining class.
	This method is left empty, but subclasses should override it when any
	special properties are involved."

	<serverEnvironment>
]
