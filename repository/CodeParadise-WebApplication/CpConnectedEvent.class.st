"
I represent the Event that a WebComponent is connected to the document's DOM.
I am non-standard, in the way that browsers normally have a callback function
for lifecycle events. In CodeParadise this is event based.
"
Class {
	#name : #CpConnectedEvent,
	#superclass : #CpEvent,
	#category : #'CodeParadise-WebApplication-Events'
}
